============
Introduction
============

This section introduces you with somthin important. 

.. math::
    P=\rho RT
    :label: ideal gas

According to ideal gas, Equation :math:numref:`ideal gas` we czan calculate absolute static pressure.

.. code-block:: c++
   :linenos:
   :emphasize-lines: 5
   :caption: the first code

   class sample ;
   for(int i=0;i<5;i++>) {
       x[i] = i*i ;
       if(X[i]>i)
          break ;
    }

.. highlight:: c++

The next patagraph is also a code::

 string a = "the string" ;
 char* b = "char series" ;
 int i = 0 ;
 double d = 2.5 ;

For highlighiting code in **Cplusplus** format you can use either ``cpp`` or ``c++``.
 
    
 




